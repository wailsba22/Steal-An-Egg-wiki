const weightRange=petWeightRange(pet.rarity),defaultWeight=((weightRange[0]+weightRange[1])/2).toFixed(1);const panel=document.createElement('section');panel.className='section tool';panel.innerHTML=`<div class="eyebrow">Weight calculator</div><h2>What will this pet earn at its hatch weight?</h2><p class="muted">Estimated model: income = base income × [1 + ((weight ÷ ${defaultWeight}kg) − 1) × ${PET_WEIGHT_FACTOR}].</p><label for="petWeight">Hatch weight (kg)</label><input id="petWeight" type="number" min="0.1" step="0.1" value="${defaultWeight}"><div class="result" id="weightResult"></div>`;document.getElementById('content').append(panel);function updateWeight(){const weight=Number(document.getElementById('petWeight').value)||0,output=base?incomeAtWeight(base,weight,Number(defaultWeight)):0;document.getElementById('weightResult').innerHTML=base?`<strong>$${output.toLocaleString(undefined,{maximumFractionDigits:2})}/second</strong><br><span class="muted">Estimated range for ${pet.name}: ${weightRange[0]}–${weightRange[1]}kg. This is a model estimate, not an official formula.</span>`:'<strong>No income model available</strong><br><span class="muted">This index entry contains hatch chance data rather than an income value.</span>'}document.getElementById('petWeight').addEventListener('input',updateWeight);updateWeight();